import React from 'react'
import { Text, View } from 'react-native'

const BackgroundImageButton = ({webViewRef, coords, filters, i}) => {

  return (
    <View style={{marginTop: 10}}>
        <Text>Nie działa V</Text>
    </View>
  )
}

export default BackgroundImageButton
